target_include_directories(app PRIVATE include)
if ((NOT CONFIG_ZMK_SPLIT) OR CONFIG_ZMK_SPLIT_ROLE_CENTRAL)
  if(CONFIG_ZMK_BEHAVIOR_OYAYUBI)
    target_sources(app PRIVATE src/behaviors/behavior_oyayubi.c)
  endif()
endif()
